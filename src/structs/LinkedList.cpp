#include "Node.cpp"
template<typename T>
class LinkedList{
    private:
        Node<T> head = NULL;
    public:
        void append(){
            Node<T> temporalNode = this->head;
            while()
        }
        
};